import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Case Studies',
  description:
    'Explore how leading organizations use AIRZEP to transform their logistics operations with measurable results.',
}

export default function CaseStudiesLayout({ children }: { children: React.ReactNode }) {
  return children
}
